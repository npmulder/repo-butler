/// <reference types="next" />
/// <reference types="next/image-types/global" />

// Standalone `tsc --noEmit` uses this stable shim; route-aware checks run
// against `next-env.d.ts` after `next typegen` via `pnpm typecheck`.
